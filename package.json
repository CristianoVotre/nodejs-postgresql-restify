{
  "name": "nodejs-postgresql-restify",
  "version": "0.1.0",
  "main": "src/server.js",
  "scripts": {
    "test": "mocha ./src/**/*.test.js --reporter spec --ui bdd --slow 30 --timeout 100000", 
    "coverage": "istanbul cover _mocha -- -R spec ./src/**/*.test.js"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "bunyan": "^1.8.1",
    "lodash": "^4.13.1",
    "path": "^0.12.7",
    "restify": "^4.1.1",
    "validator": "^5.4.0",
    "sequelize": "^3.23.6",
    "sequelize-cli": "^2.4.0",
    "pg": "^6.0.3"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^3.0.0"
  }
}
